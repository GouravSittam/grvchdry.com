services:
  portfolio_nextjs:
    image: gouravsittam/gourav-portfolio:latest
    container_name: gourav-portfolio
    working_dir: /app
    ports:
      - "3001:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
    env_file:
      - path: .env
        required: false
